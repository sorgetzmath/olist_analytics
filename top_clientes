-- Objetivo: identificar os top clientes por faturamento
-- Insight esperado: qual o cliente que mais gerou faturamento?
select
  c.customer_unique_id as cliente,
  sum(i.price) as faturamento
from customer_dataset as c
join orders_dataset as o
on c.customer_id = o.customer_id
join order_items_dataset as i
on o.order_id = i.order_id
group by c.customer_unique_id
order by sum(i.price) desc
